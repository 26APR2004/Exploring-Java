package Learning;
import java.util.*;
public class ArrayList_1 {
  public static void main(String[]args)
  {
	  ArrayList<Integer>list=new ArrayList<>();
	  list.add(1);  //Inserting data
	  list.add(2);
	  list.add(3);
	
//	  list.remove(0);   // Delete an Element
//    list.add(1,50);   // To add element at index
//	  list.set(3,20);     // To set element at index
	  for(int i=0;i<list.size();i++)   //Iteration
	  {
		  System.out.println(list.get(i));

	  }
//	  
//	  System.out.println(list.contains(2)); //Verify an element
	  
  }
}
